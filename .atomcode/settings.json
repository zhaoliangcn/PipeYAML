{
  "hooks": {
    "PostToolUse": [
      {
        "glob": "src/**/*.cpp",
        "command": "cd /home/zhaoliang/PipeYAML && cmake --build build 2>/dev/null && ctest --test-dir build --output-on-failure 2>/dev/null || echo '⚠️ Build/tests not configured yet'"
      }
    ],
    "PreToolUse": [
      {
        "glob": "PipeYAML 架构设计文档.md",
        "command": "echo '⚠️ 架构设计文档是项目核心文档，确认需要修改吗？'"
      }
    ]
  }
}
